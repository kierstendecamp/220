# strings # sequence of characters
# lists are sequences of any object [] , can be mixed data types
# len() - length

def main():
    a = "hello, world!"
    length = len(a)
    for l in a:
        print(l)
    print(length)

# indexing - grab one element out of a sequence
# index value - position within the string or list
# always start at 0


# slicing -
